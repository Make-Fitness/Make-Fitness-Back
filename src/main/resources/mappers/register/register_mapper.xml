<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="korit.com.make_fitness.mapper.RegisterMapper">
    <insert id="save" keyProperty="nowId" useGeneratedKeys="true">
        insert into now_tb
        values
        (
            default,
            #{managerId},
            #{classSubjectId},
            DATE_FORMAT(#{classTime}, '%Y-%m-%d %H'),
            #{classMaxCustomer},
            #{classCustomerReserve},
            now(),
            now()
        )
    </insert>
</mapper>